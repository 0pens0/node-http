apiVersion : apps/v1
kind: Deployment
metadata:
  name: node-azure 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-azure
  template:
    metadata:
      labels:
        app: node-azure 
    spec:
      containers:
        - name: node-azure 
          image: harbor.tanzuworld.com/openso/node
          ports:
          - containerPort: 8080